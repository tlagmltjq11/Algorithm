using System;
using System.Collections.Generic;
using System.Text;

namespace CodingTestPractice
{
    /*
    class _1037
    {
        static void Main()
        {
            int n = int.Parse(Console.ReadLine());
            int[] factors = Array.ConvertAll<string, int>(Console.ReadLine().Split(" "), s => int.Parse(s));

            Array.Sort(factors);

            Console.WriteLine(factors[0] * factors[n - 1]);
        }
    }
    */
}
